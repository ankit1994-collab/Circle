version: 2.1

jobs:
  build:
    working_directory: ~/circleci-python
    docker:
      - image: "circleci/python:3.6.4"
    steps:
      - checkout
      - run: python3 Server.app.py
  test:
    working_directory: ~/circleci-python
    docker:
      - image: "circleci/python:3.6.4"
    steps:
      - checkout
      - run: coverage run -a -m unittest Pipeline.Tests.test_data_collection

workflows:
  build_and_test:
    jobs:
      - build
      - test:
          requires:
            - build